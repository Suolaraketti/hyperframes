node_modules/
.hyperframes/
*.mp4
capture/
# Vendored locally only to render inside the network-restricted web sandbox
# (the gsap CDN host is blocked here). Not committed — index.html references it.
gsap.min.js
